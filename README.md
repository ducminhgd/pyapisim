# Installation

## Dependency

You can run `uv sync` to install dependencies.

## Run

Make sure that you migrated all the models `uv run python manage.py migrate`

Run server: `uv run python manage.py runserver`