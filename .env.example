# Django
SECRET_KEY=change-me-to-a-random-secret-key
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# HTTPS / CSRF (required when DEBUG=False and served over HTTPS)
# CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com
# HTTPS=True            # force secure cookies

# ── Database ──────────────────────────────────────────────
# Choose one by setting DB_ENGINE and uncommenting the matching block.
#
#   DB_ENGINE=sqlite       zero-config, no extra service needed
#   DB_ENGINE=postgresql   docker compose --profile postgres up
#   DB_ENGINE=mariadb      docker compose --profile mariadb up
#
DB_ENGINE=sqlite

# --- SQLite (default) ---
DB_NAME=/app/data/db.sqlite3

# --- PostgreSQL (profile: postgres) ---
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=pyapisim
# DB_USER=postgres
# DB_PASSWORD=change-me

# --- MariaDB / MySQL (profile: mariadb) ---
# DB_HOST=mariadb
# DB_PORT=3306
# DB_NAME=pyapisim
# DB_USER=pyapisim
# DB_PASSWORD=change-me
# DB_ROOT_PASSWORD=change-me-root
